# Anime Marker 技术文档

> 版本：v1.0
> 文档状态：需求已确认，进入实现阶段
> 平台：Windows 10/11（仅桌面端）

---

## 1. 需求确认

### 1.1 项目定位

Anime Marker 是一款 **Windows 桌面端**的本地动漫观看与 Bangumi 进度同步工具。

它直接管理用户本地磁盘上的动漫视频文件，提供海报墙浏览体验，调用系统已安装的 **PotPlayer** 播放，并在观看进度达到阈值（默认 95%）时，自动调用 **Bangumi API** 将该集标记为“看过”。

### 1.2 核心设计原则

| 原则 | 含义 |
| --- | --- |
| 轻量 | 不依赖 Emby / Jellyfin / Plex 等媒体服务器，无后台常驻服务 |
| 直接 | 直接读写本地文件，不搬运、不转码、不改名、不入库到第三方 |
| 自动化 | 播放、插帧、进度同步全流程自动衔接，用户零额外操作 |
| 本地优先 | 所有数据（元数据、封面、观看记录）存于本地 SQLite |

### 1.3 功能需求

| 编号 | 需求 | 优先级 | 说明 |
| --- | --- | --- | --- |
| F1 | 媒体库根目录配置 | P0 | 用户指定一个或多个动漫根目录 |
| F2 | 递归扫描视频文件 | P0 | 支持 `.mkv/.mp4/.avi/.rmvb/.ts/.flv/.mov` 等 |
| F3 | 条目识别与 Bangumi 匹配 | P0 | 依据文件夹名/文件名搜索 Bangumi 条目 |
| F4 | 封面下载与本地缓存 | P0 | 来源 `lain.bgm.tv`，缓存到 `data/covers/` |
| F5 | 海报墙展示 | P0 | 网格布局、懒加载、点击进入详情 |
| F6 | 详情页 / 集数列表 | P0 | 展示条目信息与该动漫全部集数 |
| F7 | 点击集数播放 | P0 | 启动小黄鸭 → 开插帧 → 启动 PotPlayer 播放 |
| F8 | PotPlayer 进度监控 | P0 | 轮询窗口标题，解析当前/总时长 |
| F9 | ≥95% 自动标记已看 | P0 | 每个 episode 单次播放只触发一次 |
| F10 | 手动标记兜底 | P0 | 标题解析失败时用户可手动标记 |
| F11 | 手动重新匹配条目 | P1 | 修正续集/季度匹配错误 |
| F12 | 观看状态本地持久化 | P0 | 每次轮询落库，不依赖程序退出事件 |
| F13 | 配置持久化 | P0 | Bangumi Token、路径、快捷键、代理等 |
| F14 | 日志记录 | P1 | API 调用、异常、监控关键节点 |
| F15 | 代理支持 | P1 | 供 Bangumi API 与封面 CDN 使用 |

### 1.4 非功能需求

- **性能**：媒体库首扫（1000 个文件量级）应在可接受时间内完成，扫描/匹配过程不能阻塞 UI（后台线程 + 信号回调）。
- **稳定性**：API 失败可重试，网络异常有提示，程序不崩溃。
- **可配置**：阈值、轮询间隔、快捷键、标题正则、代理均可在配置文件修改。
- **可维护**：模块分层清晰，UI 与业务逻辑解耦。

### 1.5 需求边界（明确不做）

- 不做网页端 / 移动端。
- 不内置播放器，不自行解码视频。
- 不做视频转码、剪辑、下载（非 BT/在线播放器）。
- 不实现 Bangumi OAuth 授权跳转（仅使用用户手动填入的 Access Token）。
- 不做多用户 / 账号体系 / 云同步。

### 1.6 关键假设

1. 运行环境为 Windows，且已安装 PotPlayer；`win32gui`、`pyautogui` 可用。
2. 用户在 PotPlayer 中已配置标题栏显示播放时间（见 5.5 节）。
3. Bangumi Access Token 由用户自行在 bangumi.tv 生成。
4. 单机单用户，无需并发写库控制。
5. 默认触发阈值 95%，轮询间隔 3 秒，均可在配置中修改。

### 1.7 待确认项（默认值已写入，可后改）

| 项 | 默认取值 | 备注 |
| --- | --- | --- |
| 触发阈值 | 95% | `monitor.trigger_threshold` |
| 轮询间隔 | 3 秒 | `monitor.poll_interval` |
| 是否每次强制开插帧 | 是 | `launcher.enable_ls` |
| 进度落库时机 | 每次轮询 | 不依赖关闭事件 |
| 多根目录支持 | 支持（分号分隔） | `general.library_path` |

---

## 2. 技术栈

| 层级 | 技术选型 | 说明 |
| --- | --- | --- |
| 语言 | Python 3.10+ | 主开发语言 |
| UI 框架 | PySide6 | Qt 官方绑定，LGPL 许可，适合桌面端媒体展示 |
| 播放器 | PotPlayer（外部调用） | 通过 `subprocess` 启动，不内置播放器 |
| 插帧工具 | Lossless Scaling（小黄鸭） | 通过启动器脚本联动，自动开启插帧 |
| 网络请求 | requests | 调用 Bangumi API、下载封面 |
| 进度监控 | pywin32（win32gui） | 读取 PotPlayer 窗口标题获取播放进度 |
| 自动化操作 | pyautogui / keyboard | 模拟快捷键，触发小黄鸭插帧 |
| 数据存储 | SQLite（sqlite3 标准库） | 本地缓存动漫元数据、封面路径、观看记录 |
| 配置管理 | configparser（INI）+ JSON | 存储 Bangumi Token、路径等用户配置 |
| 图像处理 | Pillow（可选） | 海报墙缩略图生成 / 尺寸适配 |
| 日志 | logging + RotatingFileHandler | 滚动日志，避免无限膨胀 |

依赖清单（`requirements.txt`）：

```
PySide6>=6.6
requests>=2.31
pywin32>=306
pyautogui>=0.9.54
keyboard>=0.13.5
Pillow>=10.0
```

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────┐
│              Anime Marker (PySide6)                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│  │  海报墙视图  │  │ 详情/集数页 │  │   设置页    │  │
│  └─────────────┘  └─────────────┘  └─────────────┘  │
│         │                │                │          │
│         ▼                ▼                ▼          │
│  ┌──────────────────────────────────────────────┐   │
│  │                业务逻辑层                     │   │
│  │  · 媒体库扫描 (scanner)                       │   │
│  │  · Bangumi API 客户端 (bangumi_api)           │   │
│  │  · 进度监控器 (monitor)                       │   │
│  │  · 播放器启动器 (launcher)                    │   │
│  │  · 数据访问层 (database)                      │   │
│  └──────────────────────────────────────────────┘   │
│         │                       │                    │
│         ▼                       ▼                    │
│  ┌──────────┐          ┌──────────────┐             │
│  │  SQLite  │          │  Bangumi API │             │
│  │  本地库   │          │  (远程 HTTP)  │             │
│  └──────────┘          └──────────────┘             │
└─────────┬───────────────────────────────────────────┘
          │ subprocess
          ▼
┌─────────────────┐  ┌──────────────────┐
│   PotPlayer     │  │ Lossless Scaling │
│   (外部进程)     │  │   (外部进程)      │
└─────────────────┘  └──────────────────┘
          │ win32gui 读取窗口标题
          └──────────────► monitor 解析进度
```

分层说明：

- **UI 层**：只负责展示与用户交互，通过信号/槽与业务层通信，不直接操作数据库或发请求。
- **业务层**：各模块职责单一，互相通过明确接口调用。
- **数据层**：SQLite 操作全部封装在 `database.py`，对外只暴露仓储方法。
- **外部进程**：PotPlayer 与 Lossless Scaling 均通过 `subprocess` 启动，本程序不接管其生命周期。

---

## 4. 项目结构

```
anime-marker/
├── main.py                    # 程序入口
├── config.ini                 # 用户配置（首次运行自动生成）
├── requirements.txt
├── app/
│   ├── __init__.py
│   ├── ui/
│   │   ├── main_window.py     # 主窗口 + 导航
│   │   ├── poster_wall.py     # 海报墙（网格、懒加载）
│   │   ├── detail_page.py     # 详情 + 集数列表
│   │   ├── settings_page.py   # 设置页
│   │   └── widgets.py         # 通用组件（海报卡片等）
│   ├── core/
│   │   ├── config.py          # 配置读取/写入
│   │   ├── scanner.py         # 媒体库扫描 + 匹配
│   │   ├── bangumi_api.py     # Bangumi API 客户端
│   │   ├── monitor.py         # PotPlayer 进度监控
│   │   ├── launcher.py        # 播放器 + 小黄鸭启动器
│   │   └── database.py        # SQLite 操作
│   └── utils/
│       ├── cover_cache.py     # 封面下载与缓存
│       ├── title_parser.py    # PotPlayer 标题解析
│       └── logger.py          # 日志初始化
└── data/
    ├── anime.db               # SQLite 数据库（运行时生成）
    ├── covers/                # 封面缓存目录
    └── logs/                  # 日志目录
```

---

## 5. 模块设计

### 5.1 媒体库扫描 `core/scanner.py`

职责：递归扫描根目录 → 识别动漫文件夹 → 匹配 Bangumi 条目 → 写入数据库。

流程：

1. 遍历 `general.library_path` 下所有视频文件，按**一级子目录**聚合为“候选条目”；根目录下的散落视频（如剧场版单文件）单独作为条目。
2. 清洗文件夹名：去除常见后缀噪声（`[1080p]`、`BDRip`、`简体`、`合集`、季度标记等），得到搜索关键词。
3. 调用 Bangumi 搜索接口，取相似度最高的结果；若中文名命中则优先。
4. 匹配结果写入 `subjects` 表，并抓取条目详情与集数。
5. 将本地视频文件按文件名中的集数序号（`01`/`第1话`/`EP01`）排序，映射到 Bangumi 集数，写入 `episodes` 表。

关键点：

- 扫描在 `QThread` 中执行，通过信号向 UI 汇报进度（`progress_changed`、`item_matched`、`finished`）。
- 匹配分数低于阈值时不自动写入，标记为“待手动匹配”。
- 已存在的 `file_path` 不重复入库（`UNIQUE` 约束 + `INSERT OR IGNORE`）。

### 5.2 Bangumi API 客户端 `core/bangumi_api.py`

职责：封装所有对 Bangumi 的 HTTP 调用，统一处理认证、代理、重试、异常。

请求头（Bangumi 强制要求 User-Agent）：

```python
headers = {
    "Authorization": f"Bearer {token}",
    "User-Agent": "AnimeMarker/1.0 (https://github.com/yourname/anime-marker)",
    "Accept": "application/json",
}
```

核心接口：

| 功能 | 方法 | 路径 | 备注 |
| --- | --- | --- | --- |
| 搜索条目 | POST | `/v0/search/subjects` | body 含 `keyword`，可用 `filter.type` 限类型 |
| 条目详情 | GET | `/v0/subjects/{subject_id}` | 含名称、封面、总集数 |
| 条目集数 | GET | `/v0/subjects/{subject_id}/episodes` | 返回该条目全部 episode id 与序号 |
| 标记单集已看 | POST | `/v0/users/-/collections/{subject_id}/episodes` | body `{"episode_id": [<ep_id>], "type": 2}` |
| 查询收藏状态 | GET | `/v0/users/-/collections/{subject_id}` | 可选，用于拉取已看状态 |

`type` 取值：`2` = 看过，「抛弃/想看/在看」等为其它枚举值。

容错策略：

- 使用 `requests.Session` + `HTTPAdapter` 配置重试（连接/读取失败重试 3 次，指数退避）。
- 代理通过 `proxies={"http": proxy, "https": proxy}` 注入。
- 失败记录日志并抛出业务异常，由调用方决定是否提示用户。

### 5.3 封面缓存 `utils/cover_cache.py`

- 输入 Bangumi 封面 URL（`lain.bgm.tv` 域名）。
- 缓存文件名使用 `subject_id + 扩展名`，避免重复下载。
- 下载失败时使用本地占位图。
- 可选：用 Pillow 生成缩略图（如 200×300）供海报墙快速加载，原图仅详情页使用。

### 5.4 播放器启动器 `core/launcher.py`

职责：一个 `play(episode)` 调用完成「开插帧 + 起播」全流程。

步骤：

1. （可配置）通过 `subprocess.Popen` 启动 Lossless Scaling。
2. 等待其窗口就绪（轮询窗口存在 + 固定延时）。
3. 用 `keyboard` / `pyautogui` 发送配置好的插帧快捷键。
4. `subprocess.Popen([player_path, video_path])` 启动 PotPlayer 播放。
5. 通知 `monitor` 开始监控该 episode。

关键点：

- 快捷键、延时、是否启用插帧均在配置中定义，避免与其他软件冲突。
- 启动外部进程时不使用 `shell=True`，路径做存在性校验。
- 播放前将 `episodes.watched` 与本次播放的 `episode_id` 注册到监控器。

### 5.5 进度监控 `core/monitor.py`

职责：轮询 PotPlayer 窗口标题，解析播放进度，达到阈值时触发标记。

**前提**：PotPlayer 默认标题不一定包含播放时间。需在 PotPlayer 的「选项 → 播放 → 窗口标题」中添加播放时间/总时长字段（如 `%playtime% / %totaltime%`）。程序提供可配置正则解析。

解析实现：

```python
# utils/title_parser.py
import re

# 默认匹配形如 "00:12:34 / 00:24:00" 或 "12:34/24:00"
TIME_RE = re.compile(r"(\d{1,2}:\d{2}(?::\d{2})?)\s*/\s*(\d{1,2}:\d{2}(?::\d{2})?)")

def parse_progress(title: str, pattern: str | None = None):
    m = re.search(pattern or TIME_RE.pattern, title)
    if not m:
        return None
    cur, total = _to_seconds(m.group(1)), _to_seconds(m.group(2))
    if total <= 0:
        return None
    return min(cur / total, 1.0)

def _to_seconds(t: str) -> int:
    parts = [int(x) for x in t.split(":")]
    while len(parts) < 3:
        parts.insert(0, 0)
    return parts[0] * 3600 + parts[1] * 60 + parts[2]
```

窗口查找（`win32gui`）：

```python
import win32gui

def find_potplayer_hwnd() -> int | None:
    result = []
    def _enum(hwnd, _):
        if win32gui.IsWindowVisible(hwnd):
            cls = win32gui.GetClassName(hwnd)
            title = win32gui.GetWindowText(hwnd)
            if "PotPlayer" in cls or "PotPlayer" in title:
                result.append(hwnd)
        return True
    win32gui.EnumWindows(_enum, None)
    return result[0] if result else None
```

监控循环：

1. `QTimer`（或独立线程）每 `poll_interval` 秒执行一次。
2. 找到 PotPlayer 窗口 → 读取标题 → 解析进度。
3. 将当前进度写入 `episodes.watch_progress`（**每次轮询都落库**，防异常退出丢进度）。
4. 若 `progress >= trigger_threshold` 且该 episode 本次会话**未触发过** → 调用 Bangumi 标记。
5. 标记成功后设置触发标志位（内存 + 数据库 `watched=1`），并刷新 UI。
6. 若窗口消失（用户关闭播放器）→ 停止监控，清理状态。

防重复触发：内存标志位 `self._triggered: set[int]` + 数据库 `watched` 双重校验。

### 5.6 数据库 `core/database.py`

- 使用标准库 `sqlite3`，`check_same_thread=False` + 线程锁（或每线程独立连接）。
- 建表 SQL 见第 6 节。
- 对外暴露仓储方法，例如：
  - `upsert_subject(subject) -> int`
  - `list_subjects() -> list[Subject]`
  - `get_episodes(subject_id) -> list[Episode]`
  - `find_episode_by_path(path) -> Episode | None`
  - `update_progress(episode_id, progress)`
  - `mark_watched(episode_id)`

### 5.7 UI 层 `app/ui/`

| 页面 | 说明 |
| --- | --- |
| `main_window.py` | 主窗口、左侧导航（海报墙 / 设置）、状态栏提示 |
| `poster_wall.py` | `QListWidget` 或 `QGridView` + 自定义卡片；封面懒加载，滚动到可视区再加载 |
| `detail_page.py` | 左侧大封面 + 条目信息，右侧集数列表（已看打勾 / 未看置灰），提供「手动标记」「重新匹配」按钮 |
| `settings_page.py` | 表单：Token、库路径、PotPlayer 路径、小黄鸭路径、阈值、轮询间隔、代理、快捷键；「保存并扫描」按钮 |

UI 与业务解耦：扫描、匹配、标记等耗时操作均在工作线程执行，完成后通过信号回传主线程更新界面。

---

## 6. 数据模型

### 6.1 `subjects`（动漫条目）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK AUTOINCREMENT | 本地主键 |
| bangumi_id | INTEGER UNIQUE | Bangumi 条目 ID |
| name | TEXT | 原名 |
| name_cn | TEXT | 中文名 |
| cover_url | TEXT | 远程封面地址 |
| cover_path | TEXT | 本地缓存路径 |
| total_eps | INTEGER | 总集数 |
| folder_path | TEXT | 对应本地目录 |
| match_state | TEXT | `auto` / `manual` / `pending` |
| updated_at | TEXT | 更新时间（ISO8601） |

### 6.2 `episodes`（集数）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK AUTOINCREMENT | 本地主键 |
| subject_id | INTEGER FK → subjects.id | 所属条目 |
| bangumi_ep_id | INTEGER | Bangumi episode ID（标记用） |
| ep_index | REAL | 集数序号（支持 12.5 等特别篇） |
| title | TEXT | 集标题 |
| file_path | TEXT UNIQUE | 本地视频路径 |
| watched | INTEGER DEFAULT 0 | 是否已看 |
| watch_progress | REAL DEFAULT 0 | 最近播放进度（0~1） |
| watched_at | TEXT | 标记已看时间 |

### 6.3 `watch_log`（可选，观看日志）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | INTEGER PK AUTOINCREMENT | 主键 |
| episode_id | INTEGER | 集数 ID |
| progress | REAL | 进度快照 |
| created_at | TEXT | 记录时间 |

建表 SQL：

```sql
CREATE TABLE IF NOT EXISTS subjects (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    bangumi_id   INTEGER UNIQUE,
    name         TEXT,
    name_cn      TEXT,
    cover_url    TEXT,
    cover_path   TEXT,
    total_eps    INTEGER,
    folder_path  TEXT,
    match_state  TEXT DEFAULT 'auto',
    updated_at   TEXT
);

CREATE TABLE IF NOT EXISTS episodes (
    id             INTEGER PRIMARY KEY AUTOINCREMENT,
    subject_id     INTEGER NOT NULL REFERENCES subjects(id) ON DELETE CASCADE,
    bangumi_ep_id  INTEGER,
    ep_index       REAL,
    title          TEXT,
    file_path      TEXT UNIQUE,
    watched        INTEGER DEFAULT 0,
    watch_progress REAL DEFAULT 0,
    watched_at     TEXT
);

CREATE INDEX IF NOT EXISTS idx_episodes_subject ON episodes(subject_id);

CREATE TABLE IF NOT EXISTS watch_log (
    id         INTEGER PRIMARY KEY AUTOINCREMENT,
    episode_id INTEGER,
    progress   REAL,
    created_at TEXT
);
```

---

## 7. 配置设计 `config.ini`

首次运行时若不存在则按默认值生成：

```ini
[general]
; 多个根目录用英文分号分隔
library_path = D:\Anime
player_path = C:\Program Files\DAUM\PotPlayer\PotPlayerMini64.exe
ls_path = C:\Program Files\Lossless Scaling\LosslessScaling.exe

[bangumi]
token =
api_base = https://api.bgm.tv
proxy =
user_agent = AnimeMarker/1.0 (https://github.com/yourname/anime-marker)

[launcher]
enable_ls = true
ls_shortcut = ctrl+alt+l
ls_start_delay = 5
player_start_delay = 1

[monitor]
poll_interval = 3
trigger_threshold = 0.95
; 留空使用内置默认正则
title_regex =

[ui]
theme = dark
poster_width = 200
```

---

## 8. 核心流程

### 8.1 首次使用流程

1. 启动 Anime Marker → 进入设置页。
2. 填入 Bangumi Access Token、媒体库根目录、PotPlayer 路径、Lossless Scaling 路径（可选代理）。
3. 点击「扫描媒体库」→ 后台线程递归扫描视频文件并匹配 Bangumi 条目。
4. 匹配成功后下载封面到 `data/covers/`，写入 SQLite。
5. 扫描完成 → 海报墙渲染展示。

### 8.2 观看与同步流程

```
用户点击某一集
   │
   ▼
launcher.play(episode)
   ├─ 启动 Lossless Scaling
   ├─ 发送插帧快捷键
   └─ 启动 PotPlayer 播放 file_path
   │
   ▼
monitor.start(episode)           ← QTimer 每 3s 轮询
   ├─ 定位 PotPlayer 窗口
   ├─ 解析标题进度
   ├─ 写入 episodes.watch_progress
   └─ progress ≥ 95% 且未触发过？
          │ 是
          ▼
   bangumi_api.mark_episode_watched(subject_id, ep_id)
          ├─ 成功 → 更新 watched=1、watched_at，UI 打勾
          └─ 失败 → 记录日志 + 提示，下次轮询可重试
   │
   ▼
用户关闭 PotPlayer → monitor 停止，流程结束
```

---

## 9. 关键实现要点

### 9.1 标记单集已看

```python
def mark_episode_watched(self, subject_id: int, episode_id: int) -> bool:
    url = f"{self.api_base}/v0/users/-/collections/{subject_id}/episodes"
    body = {"episode_id": [episode_id], "type": 2}  # 2 = 看过
    resp = self.session.post(url, json=body, headers=self.headers,
                             proxies=self.proxies, timeout=10)
    resp.raise_for_status()
    return True
```

### 9.2 防重复触发

```python
if progress >= self.threshold and ep.id not in self._triggered and not ep.watched:
    try:
        self.api.mark_episode_watched(ep.subject_id, ep.bangumi_ep_id)
        self.db.mark_watched(ep.id)
        self._triggered.add(ep.id)
        self.watched.emit(ep.id)
    except Exception as e:
        log.warning("标记失败，将在下次轮询重试: %s", e)
```

### 9.3 进度落库（不依赖关闭事件）

```python
# 每次轮询都写，避免播放器异常退出丢进度
self.db.update_progress(ep.id, progress)
```

---

## 10. 关键风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| Bangumi API 国内访问不稳定 | 匹配/标记失败 | 支持配置代理；请求自动重试 3 次；失败记录日志并提示 |
| PotPlayer 标题不含播放进度 | 监控失效（最高风险） | 文档指导用户在 PotPlayer 中配置标题模板；`title_regex` 可自定义；提供「手动标记」兜底 |
| PotPlayer 窗口标题格式变化 | 解析失败 | 正则与窗口类名匹配做兼容；解析失败时给出明确提示 |
| 小黄鸭插帧快捷键被占用 | 插帧未开启 | 快捷键在配置中可自定义；启动延时可调 |
| 条目匹配错误（续集/季度） | 标记到错误条目 | 详情页提供「重新匹配」入口，用户可手动指定 Bangumi ID |
| 播放器异常退出导致进度丢失 | 重复标记或漏标 | 每次轮询即落库；依靠 `watched` 字段判断，不依赖关闭事件 |
| 封面 CDN 无法访问 | 海报墙空白 | 使用本地占位图；封面下载失败可后续重试 |
| 外部进程路径含空格/中文 | 启动失败 | `subprocess` 使用参数列表而非字符串拼接；路径存在性校验 |
| 大媒体库扫描慢 | 界面卡死 | 扫描放入 QThread，信号汇报进度；匹配结果增量入库 |

---

## 11. 开发里程碑

| 阶段 | 内容 | 交付物 |
| --- | --- | --- |
| M1 基础框架 | 项目骨架、配置模块、日志、主窗口 | 可运行空壳程序 |
| M2 数据层 | SQLite 建表与仓储方法 | `database.py` + 单元测试 |
| M3 媒体库 | 扫描 + Bangumi 匹配 + 封面缓存 + 海报墙 | 可浏览本地动漫 |
| M4 播放联动 | 启动器（小黄鸭 + PotPlayer） | 点击集数可播放 |
| M5 进度同步 | 进度监控 + 自动标记 + 防重复 | 核心功能闭环 |
| M6 打磨 | 设置页、手动标记/重新匹配、代理、异常提示 | 可用版本 v1.0 |

---

## 12. 后续可扩展方向

- 支持更多播放器（MPV、VLC、MPC-HC），通过可插拔的监控适配器实现。
- 支持剧场版 / OVA / SP 的标记（`ep_index` 已预留 REAL 类型）。
- 观看历史统计（基于 `watch_log` 出图表）。
- 与 Bangumi 双向同步（启动时拉取已看状态并回填本地）。
- 多语言界面（Qt Translation + `.ts` 文件）。
- 自动识别季度/续集，减少手动重新匹配。
- 托盘常驻与全局快捷键。
